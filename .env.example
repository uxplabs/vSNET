# Copy to .env.local and fill in your token (never commit .env.local)
# Get token from: Figma → Settings → Account → Personal access tokens
VITE_FIGMA_ACCESS_TOKEN=

# Optional: default Figma file key (part after figma.com/design/ in the URL)
VITE_FIGMA_FILE_KEY=
